name: CI - Build & Test

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-frontend:
    name: Build Frontend
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Install workspace dependencies
        run: npm ci --workspaces --prefer-offline

      - name: Build frontend
        run: npm run build --workspace=frontend
        env:
          NEXT_PUBLIC_BACKEND_URL: https://ethical-shopping-backend.vercel.app

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: frontend-build
          path: frontend/.next
          retention-days: 7

  build-backend:
    name: Build Backend
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Install workspace dependencies
        run: npm ci --workspaces --prefer-offline

      - name: Generate Prisma Client
        run: npx prisma generate --schema=backend/prisma/schema.prisma

      - name: Build backend
        run: npm run build --workspace=backend

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: backend-build
          path: backend/dist
          retention-days: 7

  lint:
    name: Lint Code
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install workspace dependencies
        run: npm ci --workspaces --prefer-offline

      - name: Lint frontend
        run: npm run lint --workspace=frontend || true

  status:
    name: Build Status
    runs-on: ubuntu-latest
    needs: [build-frontend, build-backend, lint]
    if: always()
    
    steps:
      - name: Check build status
        run: |
          echo "Frontend Build: ${{ needs.build-frontend.result }}"
          echo "Backend Build: ${{ needs.build-backend.result }}"
          echo "Lint: ${{ needs.lint.result }}"
          
          if [ "${{ needs.build-frontend.result }}" = "success" ] && [ "${{ needs.build-backend.result }}" = "success" ]; then
            echo "✅ All builds successful!"
            exit 0
          else
            echo "❌ Some builds failed"
            exit 1
          fi
